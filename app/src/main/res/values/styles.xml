<resources>

    <!-- Base application theme. NoActionBar because we are using a Toolbar -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="BrowserTheme" parent="AppTheme">
        <item name="colorPrimary">@color/browserPrimary</item>
        <item name="colorPrimaryDark">@color/browserPrimaryDark</item>
        <item name="colorAccent">@color/browserAccent</item>
    </style>

</resources>
